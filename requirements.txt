# O SGEA não precisa de `pip install`: além da biblioteca padrão do Python, o
# servidor usa o waitress (WSGI puro-Python), que vem vendorizado na pasta
# waitress/ do próprio projeto. Este arquivo existe só para deixar isso
# explícito a quem inspeciona o repositório.
